const initialState = {

}

const userReducer = (state = initialState, action) => {
  switch (action.type) {
    case 'FETCH_BLOCKCHAIN':
      
      break;
  
    default:
      return state;
  }
}

export default userReducer